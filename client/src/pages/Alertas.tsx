import Layout from "@/components/Layout";import{api}from"@/lib/api";import{useEffect,useMemo,useState}from"react";import{Card,CardContent}from"@/components/ui/card";import{Button}from"@/components/ui/button";import{AlertTriangle,Truck,UserRound,Wrench}from"lucide-react";import{Link}from"wouter";
export default function Alertas(){const[rows,setRows]=useState<any[]>([]);const[filtro,setFiltro]=useState("TODOS");useEffect(()=>{api.get("/dashboard/alertas").then(r=>setRows(r.data))},[]);const filtered=useMemo(()=>filtro==="TODOS"?rows:rows.filter(x=>x.origem===filtro),[rows,filtro]);return <Layout><div className="mx-auto w-full max-w-6xl space-y-5"><div><h1 className="text-2xl font-bold">Central de Alertas</h1><p className="text-sm text-muted-foreground">Vencimentos de motoristas, veículos e manutenção da frota.</p></div><div className="flex flex-wrap gap-2">{[["TODOS","Todos"],["MOTORISTA","Motoristas"],["VEICULO","Veículos"],["MANUTENCAO","Manutenção"]].map(([k,l])=><Button key={k} variant={filtro===k?"default":"outline"} onClick={()=>setFiltro(k)}>{l}</Button>)}</div><div className="grid gap-3 md:grid-cols-2">{filtered.map(a=><Link href={a.href} key={a.id}><Card className="hover:bg-muted/30"><CardContent className="flex gap-3 p-4">{a.origem==="MOTORISTA"?<UserRound className="h-5 w-5"/>:a.origem==="VEICULO"?<Truck className="h-5 w-5"/>:<Wrench className="h-5 w-5"/>}<div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-2"><strong className="truncate text-sm">{a.titulo}</strong><span className={`rounded-full px-2 py-0.5 text-[11px] font-semibold ${a.nivel==="VENCIDO"?"bg-red-100 text-red-700":"bg-amber-100 text-amber-700"}`}>{a.nivel}</span></div><p className="mt-1 text-xs text-muted-foreground">{a.detalhe}</p></div></CardContent></Card></Link>)}</div>{filtered.length===0&&<div className="rounded-lg border border-dashed p-10 text-center text-sm text-muted-foreground">Nenhum alerta nessa categoria.</div>}</div></Layout>}
